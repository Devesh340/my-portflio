EMAIL_USER=yourgmail@gmail.com
EMAIL_PASS=your_app_password

# Notes:
# - For Gmail you must create an App Password (if 2FA is enabled) and use that as EMAIL_PASS.
# - Never commit a real .env with secrets to source control. Use environment variables in production.
